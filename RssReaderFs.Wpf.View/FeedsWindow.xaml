<Window x:Class="RssReaderFs.Wpf.View.FeedsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Feeds" Height="300" Width="300"
        Visibility="{Binding Visibility}"
        Closing="Window_Closing"
        FontFamily="Yu Gothic">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <ListView x:Name="listView" ItemsSource="{Binding Feeds}" Grid.Row="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name"   DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Url"    DisplayMemberBinding="{Binding Url}" />
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add Feed" Command="{Binding AddFeedCommand}" Width="80" Height="20" HorizontalAlignment="Right" Margin="10" Grid.Row="1" />
    </Grid>
</Window>
